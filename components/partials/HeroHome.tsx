import HeroContent from "../common/HeroContent";
import { AuroraBackground } from "../ui/bgAurora";

const HeroHome = () => {
  return (
    <AuroraBackground>
      <div>
        <HeroContent />
      </div>
    </AuroraBackground>
  );
};

export default HeroHome;
